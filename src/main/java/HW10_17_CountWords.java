public class HW10_17_CountWords {

    /**Напишите алгоритм CountWords, который принимает на вход текст и слово,
     *     и считает все вариации этого слова в тексте:
     *      “As of March 2022, Java 18 is the latest version, while Java 17, 11 and 8 are the current long-term support (LTS)
     *     versions. Oracle released the last zero-cost public update for the legacy version Java 8 LTS in January 2019 for
     *     commercial java use, although it will otherwise still support Java 8 with public updates for
     *     personal use indefinitely. Other vendors have begun to offer zero-cost builds of OpenJDK 8 and 11 that
     *     are still receiving security and other upgrades.”, “Java” → 5
     *
     *      “As of March 2022, Java 18 is the latest version, while Java 17, 11 and 8 are the current long-term support
     *             (LTS) versions. Oracle released the last zero-cost public update for the legacy version Java 8 LTS
     *     in January 2019 for commercial java use, although it will otherwise still support Java 8 with public updates for
     *     personal use indefinitely. Other vendors have begun to offer zero-cost builds of OpenJDK 8 and 11 that
     *     are still receiving security and other upgrades.”, “version” → 2
     */

    public int countWords(String text, String word) {
        int count = 0;

        return count;
    }

}
